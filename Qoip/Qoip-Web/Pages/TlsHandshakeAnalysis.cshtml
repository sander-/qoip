@page
@model Qoip.Web.Pages.TlsHandshakeAnalysisModel
@{
    ViewData["Title"] = "TLS Handshake Analysis";
}

<section class="container mt-5">
    <h3 class="mb-4">TLS Handshake Analysis</h3>

    <div id="app">
        <form v-on:submit.prevent="performRequest" class="needs-validation" novalidate>
            <div class="mb-3">
                <label for="host" class="form-label">Host:</label>
                <input type="text" id="host" v-model="host" class="form-control" required>
                <div class="invalid-feedback">
                    Please enter a host.
                </div>
            </div>
            <div class="mb-3">
                <label for="port" class="form-label">Port:</label>
                <input type="number" id="port" v-model="port" class="form-control" required>
            </div>
            <div class="mb-3">
                <label for="timeout" class="form-label">Timeout (ms):</label>
                <input type="number" id="timeout" v-model="timeout" class="form-control" required>
            </div>
            <button type="submit" class="btn btn-primary" :disabled="loading">Submit</button>
            <button type="button" class="btn btn-secondary ms-2" v-on:click="clearForm">Clear</button>
        </form>
        <div v-if="loading" class="mt-3">
            <div class="spinner-border" role="status">
                <span class="visually-hidden">Loading...</span>
            </div>
        </div>
        <div v-if="response" class="mt-4">
            <div class="d-flex justify-content-between align-items-center mb-2">
                <h2 class="h5 mb-0">TLS Handshake Analysis Response</h2>
                <button type="button" class="btn btn-outline-secondary btn-sm" v-on:click="copyResponse">{{ copied ? 'Copied' : 'Copy JSON' }}</button>
            </div>
            <pre class="bg-dark p-3 border rounded">{{ formatJson(response) }}</pre>
        </div>
        <div v-if="error" class="mt-4 alert alert-danger">
            {{ error }}
        </div>
    </div>
</section>

@section Scripts {
    <script type="module" src="~/js/tlshandshakeanalysis.js" asp-append-version="true"></script>
}
